schema_version: 1
name: auth
description: Authentication and authorization — user identity, session management, access control, and API security.

adds:
  workflows:
    - security-review
  skills:
    - security-policies
  stack_sections:
    - Authentication
    - Authorization
  project_defaults:
    auth_provider: "none"
